# Architecture

Student → React/Capacitor App → Supabase Auth → Supabase Edge Functions → AI Provider →
Structured JSON (Zod-validated) → PostgreSQL (Supabase) → Student Dashboard

See the shared architecture plan for full detail (schema, endpoints, prompt structure, roadmap).
